@inherits LayoutComponentBase
@using MyJournal.Components.Pages.Auth
@using MyJournal.Services
@using MyJournal.Components.Shared

@inject ThemeService Theme
@implements IDisposable

<FluentToastProvider />
<FluentDialogProvider />
<FluentTooltipProvider />
<FluentMessageBarProvider />
<FluentMenuProvider />

<MyJournal.Components.Shared.Toast />

<AuthGuard>

    <div class="page @(Theme.IsDarkMode ? "dark-mode" : "")">

        <div class="sidebar">
            <NavMenu />
        </div>

        <main class="content">
            @Body
        </main>
    </div>

</AuthGuard>

